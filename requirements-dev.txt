-r requirements.txt
pytest>=7.0.0
pytest-cov>=6.0.0
black>=23.0.0
flake8>=7.0.0        # ← Aligner avec pre-commit
isort>=5.12.0
autoflake>=2.0.0
pre-commit>=3.3.0
# boto3 is now installed with awscli in Dockerfile to avoid version conflicts
